package com.vonallin.lib.jump;

import android.app.Activity;
import android.content.Intent;


/**
 * add by zhaoshuchao
 */
public abstract class AllinJumper {
//    /** 内部跳转，extra key */
//    public static final String EXTRA_INNER_JUMP_STR = "innerJumpStr";
//    /**
//     * 使用schema进行内部跳转
//     */
//    public static void innerJumpBySchema(Activity activity, String jumpUrl) {
////        if (activity == null) {//补偿
////            activity = BaseApplication.topActivity;
////        }
////        if (activity == null) {//补偿后还为空，不执行跳转
////            return;
////        }
////        //阻止跳转
////        if (PushURLDataParser.getInstance().parserURLData(jumpUrl) == null) {
////            return;
////        }
////        if (GlobalApp.isAppLaunched && GlobalApp.topActivity != null) {
////            Intent intent = new Intent();
////            intent.putExtra(EXTRA_INNER_JUMP_STR, jumpUrl);
////            activity.setIntent(intent);
////            LufaxPushOrSchemaJumpDispatcher.dispatchAction(activity);
////        } else {
////            ComponentSender.sendGoWelcomeActivity(activity, -1);
////        }
//    }

}
